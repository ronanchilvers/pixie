---
composer:
  after:
    - echo "hallo" > console.out
shared:
  folders:
    - tmp
writables:
  paths:
    - tmp
clear_paths:
  paths:
    - .gitignore
    - deploy.yaml
    - gulpfile.js
    - LICENSE
    - package.json
    - README.md
