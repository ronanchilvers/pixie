---
composer:
  before:
    - echo "before hook" > console.out
  after:
    - echo "after hook" >> console.out
shared:
  folders:
    - tmp
writables:
  paths:
    - tmp
clear_paths:
  paths:
    - .gitignore
    - deploy.yaml
    - gulpfile.js
    - LICENSE
    - package.json
    - README.md
cleanup:
  keep_deployments: 10
